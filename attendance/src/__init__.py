# Local editor-only stub package for src.* imports
# These files are stubs to satisfy the static analyzer in the editor.
# At runtime, the real `src` package from the Silent-Face-Anti-Spoofing repo will be preferred when available.
